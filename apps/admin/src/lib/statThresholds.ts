/** Products with stock_disponible <= this value are flagged as low stock. */
export const LOW_STOCK_THRESHOLD = 5;

/** Administrations with this status are counted as active. */
export const ACTIVE_STATUS = 'active';
